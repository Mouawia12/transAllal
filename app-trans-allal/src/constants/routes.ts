export const appRoutes = {
  signIn: '/sign-in',
  home: '/(driver)/(tabs)',
  trip: '/(driver)/(tabs)/trip',
  tracking: '/(driver)/(tabs)/tracking',
  profile: '/(driver)/(tabs)/profile',
  settings: '/settings',
} as const;
